# Production build points the app (REST + sockets) at the Render backend.
# Vite inlines this at build time, overriding the localhost value in `.env`.
VITE_API_BASE_URL=https://huddle-9fmz.onrender.com
